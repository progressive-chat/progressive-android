# core-bridge (WIP, not wired)

Skeleton for docs/PCORE_PORT_PLAN.md Phase 1. Do NOT add to
settings.gradle yet — this module must stay out of release builds until
`PcoreBridge.ping()` works against a real libpcore on device.
