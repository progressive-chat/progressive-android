# PCORE USAGE INVENTORY (Phase 0)

Generated by scanning app sourceSets for `import org.matrix.android.sdk.*`.

- **SDK import lines:** 2836
- **Files touching the SDK:** 858
- Machine-readable: [`docs/pcore_usage_inventory.json`](pcore_usage_inventory.json)

## By subsystem

| Subsystem | Imports | Port phase |
|---|---|---|
| session (rooms/timeline/crypto/media/identity…) | 1904 | 3–6 (bulk of the port) |
| util + extensions + query (Kotlin sugar over SDK types) | 509 | dies with the SDK — replaced by core state |
| auth | 151 | 2 |
| flow (reactive wrappers) | 92 | 3 (core pushes state instead) |
| failure (error model) | 79 | 2 (bridge maps to app errors) |
| misc (patterns/raw/account/securestorage/metrics…) | ~101 | 2–7 scattered |

## Top classes (what the bridge must expose first)

| Class | Import sites |
|---|---|
| `Session` | 213 |
| `MatrixItem` / `toMatrixItem` | 100+75 |
| `RoomSummary` | 92 |
| `getRoom` / `getRoomSummary` | 72+25 |
| `toModel` | 48 |
| `EventType` | 42 |
| `Event` | 30 |
| `Failure` | 25 |

## Reading

The port is dominated by ONE seam: everything flows through
`Session.getRoom(...)` and the returned room-level APIs. A bridge that
models **Session → RoomScope** covers the majority of call sites;
timeline/event models come next.
